{% extends 'layout.njk' %}

{% block content %}

<div class="card">
    <table>
        <thead> {# Cabeçalho #}
            <tr> {# Linha #}
                <th>Instructor</th> {# Colunas #}
                <th>Atendimento</th> {# Colunas #}
            </tr>
        </thead>
        <tbody> {# Corpo da Tabela #}
            {% for instructor in instructors %}
                <tr> {# Linha #}
                    <td>{{ instructor.name }}</td> {# Colunas #}
                    <td>{{ instructor.services }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{% endblock content %}